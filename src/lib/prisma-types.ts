// Central place to import Prisma types with path alias
// Re-export the Prisma namespace and model types from the generated client
export type { Prisma, User, Application, ApiKey, Image, ImageVariant, Session, AuditLog, Account, VerificationToken } from "../../prisma/generated/prisma"
