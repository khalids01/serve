// Central place to import Prisma types with path alias
// Re-export the Prisma namespace and model types from the generated client
export type { Prisma, User, Application, ApiKey, Image, ImageApplication, ImageVariant, Session, AuditLog, Account, VerificationToken } from "../../prisma/generated/prisma"
